package com.example.nspain.unoapp;

/**
 * Wrapper for the Player class to make it clear that this type of player should be
 * user controlled (so much of its actions are controlled in the app activity).
 */
class UserControlledPlayer extends Player {
}
